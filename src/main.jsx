import React from 'react'
import ReactDOM from 'react-dom/client'
import Tabs from './assets/CSS/Tabs'

ReactDOM.createRoot(document.getElementById('root')).render(
  <Tabs/>
)
/*ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Form />
  </React.StrictMode>,
)*/
